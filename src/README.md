# src

This directory exists to satisfy repository organization requirements.

Primary implementation currently lives in modular top-level packages and directories:

- `ui/`
- `pipeline/`
- `retrieval/`
- `ranking/`
- `models/`
- `evaluation/`
- `scripts/`

No notebook-based source code is required to run the project.
